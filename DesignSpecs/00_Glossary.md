---
file: DesignSpecs/00_Glossary.md
role: domain_glossary
status: ACTIVE
version: v0.6 (2026-08-31)
last_updated: 2026-08-31
summary: 全域字典與領域知識庫,兩區塊:顆粒度字典 + 領域背景與已確認決策。(v0.6:E1 / E5 / E7 回填落檔——DEC-001 補實值背書、DEC-004 補測量雙路徑、新增 DEC-011 權限雙層機制;清除 DEC-004 後方殘留空白範本表)(v0.5:追加 DEC-009 SYS02 共用單一角色命名空間、DEC-010 SYS02 下切 SS03 / SS04)(v0.4:追加 DEC-007 as-is 修正已完成、DEC-008 「不上架」語意)(v0.3:追加 DEC-005 場域二層命名、DEC-006 SYS01 下切 SS 層)(v0.2:R1 階段 SA 裁示 DEC-001 ~ DEC-004 落檔)
---
# 全域字典與領域知識庫

## 區塊A:顆粒度字典

| 術語 | 定義 |
|------|------|
| Epic | 最大的功能價值單元,由多個 UserStory 構成的業務閉環。 |
| UserStory | 以「As a [角色], I want [行動], so that [業務目標]」描述的使用者需求單元。 |
| Function | 可獨立交付的功能單位,對應一個 F 功能模組。 |
| AC (Acceptance Criteria) | 驗收標準,定義功能「完成」的可判定條件。 |
| AT (Acceptance Test) | 驗收測試,對應 AC 的可執行測試情境。 |
| EdgeCase | 邊緣情境,描述非主線的例外、邊界與異常條件。 |

## 區塊B:領域背景與已確認決策

<!-- 別名:領域背景知識 -->

> 本區塊收納「決策的背後理由」與「AI 需知的領域背景」。規格的最新狀態在節點文件裡,此處不重複維護。
> 矛盾的原始證據與比對過程見 `05_SourceAudit.md`。

### DEC-001 角色權限的 SSOT 為 `D_OriginUser.Role`

**決策**:角色驗證統一走 `D_OriginUser.Role`(每個 UID 在每個 OID 下持有一個 Role)。
角色值域來自 `D_C_Role` 代碼表。

**`D_UserList.Role` 為待移除的殘留欄位**,其欄位描述「1:業主 2:組長 3:組員」是舊版三角色制的遺留,**不具效力**,不得作為任何規格的依據。DB Schema(2026-07-30)尚未完成清理。

**理由**:`樹碳集_系統管理設計v6.pptx` 第 25 頁載明 6/17 結論——
「UserList.Role移除!無論產品或是落地板(Tpark)交付,統一使用 OriginUser 驗證角色與權限。」
同檔第 13 頁定義「User 於每個 OID 都有一個角色權限(Role)」,故 Role 必須掛在 OID×UID 的關聯表上,而非掛在全域唯一的 UserList。

**角色值域**(據 `系統管理設計v6` p47 情境表、p81 測試帳號表交叉佐證;p59 註明帳號管理下拉選單僅顯示 2~7,故 Role 1 不可由介面指派):

| Role | 角色 | 可視範圍 |
|---|---|---|
| 1 | 母帳號(業主) | 全視域 |
| 2 | 系統管理員 | 全視域 |
| 3 | 業主長官 | 全視域 |
| 4 | 廠商負責人 | 依 GID |
| 5 | 廠商組長 | 依 GID |
| 6 | 廠商組員 | 依 GID |
| 7 | 測量檢驗員 | 依 GID |

可視性分界:`Role<4` 全視域,`Role>3` 依 GID 決定可視內容(案場、分區、植栽、任務)。

**術語校正**:xmind 功能架構圖寫「測量檢**測**員」為錯字,正確為「測量檢**驗**員」。

**✅ 實值核對完成(2026-08-31,E1 回填)**:工程端提供 `D_C_Role` 實際資料列,與上表**逐列吻合**,`State` 全為 1(無停用角色)。唯一補正:`SID=1` 的正式名稱為「**母帳號(業主)**」,已回填上表。原「待補」事項結案,`02_Scope.md` 的 `[!TBD-ROLE-01]` 同步解除。

**✅ 工程端佐證(2026-08-31,E5 回填)**:「目前只判定 `OriginUser` 的 Role 來決定,`UserList` 表的 Role 是舊的設計沒刪除」——與本決策及 6/17 結論完全一致,`D_UserList.Role` 為未刪除的舊設計確認屬實。

- 裁示人:SA｜日期:2026-08-22｜來源:R1 矛盾浮現 C01
- 實值核對:E1 / E5 回填(Rossi,2026-08-31),見 `00_START_SA/EngRegister.md`

### DEC-002 任務狀態機以實作為準,`任務派工v12.2` p56 判斷式已失效

**決策**:`樹碳集_任務派工系統v12.2.pptx` 第 56 頁的十狀態判斷式**不作為規格依據**,予以排除。工程端已採用其他方式實作。

**背景**:該頁判斷式引用 `SubmitID` / `SubmitNote` / `ApplyID` 三個欄位,`L_MissionList` 均不存在(另有 `FinnishTime` 拼字錯誤,DB 實為 `FinishTime`)。原判定為「狀態機不可實作」,經 SA 裁示為文件過時,非實作缺口。

**下游影響**:十個任務狀態的**語意與流轉順序**仍然有效(待分配→待審核→退回→已排班→執行中→執行完畢→逾期→申請延期→延期→結案),但**判斷條件**須向工程端取回實際邏輯後才能寫入 AC。在取得前,相關節點的判斷條件一律標 `[!TBD]`,不得照抄 p56。

- 裁示人:SA｜日期:2026-08-22｜來源:R1 矛盾浮現 C08

### DEC-003 固碳量為計算值,不落資料表

**決策**:固碳量 / 二氧化碳當量 / 預估年吸存量皆為**計算值**,由程式內公式或 DB view 即時運算,不以欄位形式儲存。故 DB Schema 無對應欄位屬設計選擇,非缺口。

**下游影響**:計算式與樹種係數的 SSOT 位於程式碼或 DB view 定義,**不在本專案素材內**。撰寫碳匯相關 AC 時須向工程端取回:
1. 固碳量計算式(輸入欄位、係數來源)
2. 生物量數值的取得方式——`樹碳集_測量的新刪修.pptx` p3 測量欄位表中「生物量數值」自標「待確認」,此項是否已解決待查
3. 樹種係數的掛載位置(`A_TreeKind` 現有 18 欄無係數欄位)

在取回前,碳匯相關規格一律標 `[!TBD]`。

- 裁示人:SA｜日期:2026-08-22｜來源:R1 矛盾浮現 C11

### DEC-004 維養紀錄尚未實作

**決策**:維養紀錄功能**尚未實作**,DB 無獨立維養表為現況實情(as-is),非文件與實作的矛盾。

**下游影響**:此為 to-be 範圍。功能架構圖 4-1 歷程記錄→維養列表(6 大類 + 13 項工法明細)與 8. 資料管理模組→維養紀錄管理,兩處皆屬新建。
`樹碳集_測量的新刪修.pptx` p2 載明維養須支援兩條填報路徑——(1) 任務日誌接入 (2) 單木植栽直接填寫;測量已靠 `G_SurveyRecord.MID` 可為空支援路徑 (2),維養的資料落點需在 SD 階段新設計。

**✅ 工程端確認雙路徑適用範圍(2026-08-31,E7 回填)**:確認 DB 無維養紀錄專用表、無 schema 草案。並補充實情——

> 任務紀錄與維養紀錄應該是互相關聯。舉例來說,測量記錄可以單獨在植栽管理新增,也可以在任務紀錄中被新增。

即**雙填報路徑不只適用維養紀錄,測量紀錄亦同**(且測量的雙路徑已實作)。M04-F01 維養紀錄設計時,應以現行測量的雙路徑為既有範式對齊,而非另創機制。

- 裁示人:SA｜日期:2026-08-22｜來源:R1 矛盾浮現 C10
- 現況確認:E7 回填(Rossi,2026-08-31)


### DEC-005 場域二層結構:`E_LocationList` = 案場,`E_ProjectList` = 分區

**決策**:場域為兩層結構。上層「**案場**」對應 `E_LocationList`,下層「**分區**」對應 `E_ProjectList`。

⚠️ **此命名與英文表名的直覺相反**,任何規格、API、UI 文案引用時一律以本決策為準,不得依表名望文生義。

**支撐證據**(六條,互相咬合):

| # | 證據 | 推得 |
|---|---|---|
| 1 | `E_ProjectList.LID` → `E_LocationList.SID` | Project 隸屬 Location,故 Location 為上層 |
| 2 | `E_LocationList.OID` | 案場掛在組織(母帳號)下 |
| 3 | `E_ProjectList.GroupID` → `D_Group.SID` | 分區才指派廠商群組;對應 xmind 3-1「分區管理→基本資料→團隊」 |
| 4 | `E_ProjectList.X_84` / `Y_84` 中心點坐標(`E_LocationList` 無此欄位) | 對應 xmind「分區管理→中心點坐標」 |
| 5 | `E_GeoData.PID` → `E_ProjectList.SID`,存 `Geom` 範圍多邊形 | 對應 xmind「分區設定→圖台工具 / 範圍名稱」 |
| 6 | `E_ProjectTrees.PID`(分區)↔ `TID`(植栽);`系統管理設計v6` p46「樹木資料綁在分區」 | 植栽掛分區,不掛案場 |

**衍生業務規則**:
- 植栽歸屬鏈為 `組織 OID → 案場 → 分區 → 植栽`,植栽不可直掛案場。
- 廠商群組(GID)的權限邊界落在**分區**層,非案場層。
- `系統管理設計v6` p46:「系統管理員才能新增案場與分區」;試用帳號需具備此能力,但轉為協力廠商後不得新增案場。

**術語對照表**(供 UI 文案與 API 命名統一):

| 業務用語 | 資料表 | 英文建議用語 |
|---|---|---|
| 案場 | `E_LocationList` | Location(**不是** Site/Project) |
| 分區 | `E_ProjectList` | Project(**不是** Zone/Area) |
| 分區範圍 | `E_GeoData` | GeoData |

- 裁示人:SA｜日期:2026-08-22｜來源:R1 矛盾浮現 C03

### DEC-006 SYS01 下切 SubSystem:完整版與調查版分屬 SS01 / SS02

**決策**:樹碳集主系統(SYS01)下切兩個 SubSystem——
- **SS01 完整版**:Web 管理端,根路由
- **SS02 調查版**:外業行動端,`/mobile/` 路由根目錄

**理由**:SA 裁示依據為「不同 URL」。此符合 `AI_Rules.md` §4 的 SubSystem 技術觸發條件之一——**獨立前端 App 或路由根目錄**。

**證據**:`樹碳集_任務派工系統v12.2.pptx` 內任務通知信的調查版連結格式為
`/mobile/MissionList.SID`、`/mobile/MissionList.SID/L_DailyReport.SID`,
`/mobile/` 於素材中出現 7 次,確為獨立路由根目錄。
另 `樹碳集_調查版_任務管理v3.1.pptx` 為調查版專屬設計文件,
`任務派工v12.2` p56 / p67 均有「完整版 / 調查版」逐功能對照表,兩版功能集合不同。

**衍生**:功能歸屬須逐項判定屬 SS01 / SS02 / 兩者共用。已知對照來源為
`任務派工v12.2` p56(十狀態的完整版 / 調查版可用性)與 p67(角色 × 功能的版本歸屬)。

- 裁示人:SA｜日期:2026-08-22｜來源:R1 矛盾浮現 C06


### DEC-007 as-is 修正已完成,本次 SA 範圍為純 to-be

**決策**:`2025Tpark初驗結果.pptx` 所列初驗缺失(學名屬種名須斜體、命名者正體;通知信主旨須帶「GreenPedia植譜誌」、簽名檔須放 LOGO)**已修正完畢**,不納入本次 SA 範圍。

**衍生的關鍵前提**:現行樹碳集系統**已上線並通過初驗**。故 xmind「完整版」不是從零新建的系統,而是**在既有已上線系統上的擴充與補完**。

此前提直接影響 R2 的優先級判定——素材中的功能有三種狀態,不可一律當作待開發:

| 狀態 | 已知實例 | 判定依據 |
|---|---|---|
| 已實作 | 任務管理狀態機、測量紀錄、固碳量計算 | DEC-002 工程已用他法實作;`G_SurveyRecord` 已有表;DEC-003 公式已在程式 / view |
| 未實作 | 維養紀錄、儀錶板 | DEC-004;DEC-008 |
| 未知 | xmind 其餘節點 | 待取回(見 `00_Status.md` E4) |

> ⚠️ R2 產出 P1 / P2 / P3 優先級前,必須先取得 xmind 節點的「已實作 / 未實作」對照,否則會把已上線功能誤列為待開發。

- 裁示人:SA｜日期:2026-08-22｜來源:R1 矛盾浮現 C04

### DEC-008 xmind「不上架」= 尚未開發完成,不等於範圍排除

**決策**:功能架構圖中的「不上架」標註,語意為「**該功能尚未開發完成**」,**不是** Out-of-Scope。標註此字樣的功能仍在本次 SA 範圍內。

**適用實例**:
- 2. 總覽頁→「儀錶板(不上架,預留版面)」——**要繼續執行**,尚未開發完成。
- 6-1 帳號管理→「(預設四個角色,未來可能可以調整,現階段不上架)」——角色自訂功能同屬未完成,非排除。

**下游影響**:
- 上述功能列入 In-Scope,優先級依開發狀態落在 P2 / P3,**不得標 `(Out of Scope)`**,亦不得指向 `W99` 佔位符。
- 對應的 W## 節點在 `03_Structure.md` 預登記,狀態設 `PLANNED`(依 `AI_Rules.md` §3.5:PLANNED 是「還沒做」,PENDING 是「卡住了」),不建立實體節點單檔,待 SA 決定展開時再補建。

**術語校正**:後續文件一律不使用「不上架」描述範圍,改用「未開發完成」;範圍排除一律用 `Out of Scope`。避免兩者混淆。

- 裁示人:SA｜日期:2026-08-22｜來源:R1 矛盾浮現 C05

### DEC-009 SYS02 與 SYS01 共用單一角色命名空間

**決策**:訂閱管理平台(SYS02)**不擁有獨立的 Role 命名空間**。全系統共用 `D_C_Role` 單一值域,`D_OriginUser.Role`(UID × OID → Role)仍為唯一 SSOT,延續 DEC-001。

SYS02 在角色體系中只擁有一項專屬職權:**Role=1 與組織 Token 的綁定關係**(綁定 / 解除)。成員的實際角色值由 SYS01 的角色體系決定。

**判定依據**(四條):
1. **Role=1 / Role=2 兩邊一致** —— `訂閱平台v1.pptx` p2 路徑三「綁定第一個 Role=1」、情境流程「老闆在訂閱平台內將 IT 主管設為 Role 2(系統管理員)」,與 `D_C_Role` 的 1=母帳號、2=系統管理員完全對應。
2. **樹碳集在讀同一個欄位** —— p4 判斷表「`Role≠1` 業務系統不顯示跳轉按鈕」;p6 明載「**業務系統前端判斷**:此 UID 在此 OID 的 Role」。做判斷的是 SYS01,讀的是 `D_OriginUser`。
3. **DB 無第二張角色表** —— 58 表全掃,含 Role 語意者僅 `D_C_Role`(代碼表)、`D_OriginUser.Role`(SSOT)、`D_UserList.Role`(DEC-001 判定為待清理殘留)。獨立命名空間在現行 DB 無載體。
4. **UI 稿不認第二套值** —— p11 成員管理的角色標籤只有「管理員 `Role=1`」與「一般成員」(後者不標 Role 值),全份 UI 稿未出現「5」。

**衍生裁示:`訂閱平台v1.pptx` p2 路徑四的「Role=5」判為失效引用,予以排除。**

處理方式比照 DEC-002(`任務派工v12.2` p56 判斷式過時被排除)。原文為「B2B 員工被邀請 → SSO 登入 → 自動加入 OID 為 Role=5」,不予採用,理由:
- `D_C_Role` 5 = 廠商組長,但路徑四的對象是**業主組織的員工**,語意不合。
- 該流程未指派 Group,Role=5 且無 Group **直接違反 L2 / L3**(「User 在 OID 內沒有 Group 時,Role 必須 `<4`」,`02_Scope.md` 限制與假設)——照字面實作為非法狀態。
- p11 UI 稿不支持該值。

**下游影響**:
- 「被邀請成員進入 OID 後的預設 Role 與 Group 指派規則」為真實未知,列為工程前置決策點 **E12**(`06_HandoffPackage.md` §6)。取回前 EP13 成員管理的 AC 一律標 `[!TBD]`,不得照抄路徑四。
- `02_Scope.md` 的 `[!TBD-ROLE-04]` 由「命名空間是否獨立」改寫為「邀請預設角色與 Group 指派待取回」,解鎖條件指向 E12。
- 角色值域本身仍待 E1 取回(`D_C_Role` 實際資料列),本決策不解除 `[!TBD-ROLE-01]`。

**待對齊(不阻塞本決策)**:p2 文字流程稱可在訂閱平台設 Role 2,但 p11 UI 稿只有「設為管理員」(=Role=1)一個動作,無設 Role 2 入口。文字與 UI 稿落差,於 EP13 展開時對齊。

- 裁示人:SA｜日期:2026-08-31｜來源:R1 矛盾浮現 C01-b

### DEC-010 SYS02 下切 SubSystem:崧旭業務後台 SS03 / 組織管理者後台 SS04

**決策**:SYS02 訂閱管理平台**下切兩個 SubSystem**,SS 編號採**全域唯一**。

| SS | 名稱 | 使用者 | 憑證載體 | 登入方式 |
|---|---|---|---|---|
| SS03 | 崧旭業務後台 | 崧旭內部營運人員 | `M_AdminList`(Account / Password / Status) | 帳號密碼 |
| SS04 | 組織管理者後台 | 客戶端組織管理者 | `D_OriginAccount.UserKey` + `Password` | 路徑 A 輸入組織 Token(`sk-org-…`);路徑 B 自 SYS01 跳轉帶 OID·UID·Token context |

M 層歸屬:`M01 業務營運後台` → SS03;`M02 組織自助管理` → SS04。

**判定依據**:
- **獨立權限邊界 / 獨立登入**(`AI_Rules.md` §4 觸發條件第 2 項)—— 兩套憑證分屬不同資料表,且皆獨立於 `D_UserList` 的 UID 帳號體系。DB 欄位註解白紙黑字:`D_OriginAccount.UserKey` =「母帳號登入用Key」、`Password` =「母帳號密碼」。
- **無共用畫面** —— `訂閱平台v1.pptx` p9 ~ p12 顯示 SS04 的左側導覽固定為「訂閱總覽 / 帳務 & 付款 / 成員管理 / Token 設定」四項;SS03 的兩項功能(試用申請審核、訂閱用戶管理)不在其中,素材中無任何畫面同時出現兩邊功能。兩者為各自完整的 App,僅共用資料庫。
- 使用者群完全不重疊,無單一帳號可同時進入兩邊。

**編號規則**:SS 編號**全域唯一**,不採各 SYS 自行編號。SYS01 已由 DEC-006 鎖定 SS01(完整版)/ SS02(調查版),故 SYS02 續編 SS03 / SS04。
理由:SS 不進節點 ID(節點恆為 `M##-F##-W##`),全域唯一的成本為零;而 `03_Structure.md` / `04_FuncMap.md` 會同時列出所有 SS,重號將造成引用混淆。
> 注意:`05_SourceAudit.md` C06-b 原文曾以「SS01 崧旭後台 / SS02 組織管理者後台」記述,屬 R1 隨手記,無裁示效力,以本決策為準。

**實作成本說明**(供階段 2 參照):下切 SS **不建立獨立目錄**。依 `20_Setup.md` 目錄命名規則,`SS##_overview.md` 置於 `SYS##/` 下;M 層目錄亦直接住在 `SYS##/` 下(`AI_Rules.md` §4.5 物理層級對照表)。故加 SS 僅影響四處:新增 `SS##_overview.md`、`M##_overview.md` 的 `parent_subsystem` 欄位、`03_Structure.md` 多一層標題、`04_FuncMap.md` 功能樹多一層。**無實體檔案搬移,M / F 編號與節點 ID 不受影響。**

**下游影響**:
- 解除 `[!TBD-LAYER-02]`(`01_Strategy.md` / `02_Scope.md` / `06_HandoffPackage.md` §4)。
- 階段 2 `21_AddModule.md` 建 SYS02 結構時,依本表填 `parent_subsystem`。
- SYS02 合計 SS 數 2、M 數 2、F 數 6,不變。

- 裁示人:SA｜日期:2026-08-31｜來源:R1 矛盾浮現 C06-b

### DEC-011 權限採雙層機制:功能授權矩陣 + Role×GID 資料範圍

**決策**:系統權限**不是**「硬編碼 `Role<4`」與「`A_Function`/`A_Right` 資料驅動矩陣」二選一,而是兩者分工的**雙層機制**。

| 層 | 載體 | 管什麼 |
|---|---|---|
| **功能授權層** | `A_Function`(模組 / 頁面 / 按鈕三層)+ `A_Right`(角色 × 功能) | **能不能用這個功能** —— 選單、頁面、按鈕的可見與可用 |
| **資料範圍層** | `D_OriginUser.Role` + `D_GroupUser.GID` | **這個功能能碰到哪些資料** —— 案場、分區、植栽、任務 |
| **交界** | `A_Right.Relation` | 矩陣對資料層下的**額外限縮** |

**`A_Right.Relation` 語意**(2026-08-31 工程端提供):欄位備註為「**是否只能使用帳號關聯資料**」。

| 值 | 語意 |
|---|---|
| `0` | 該功能不受帳號關聯限制 |
| `1` | 該功能**只能操作與自己帳號關聯的資料**(即受 GID 限縮) |

**實資料佐證**:`A_Right` 159 列中僅兩列為 `Relation=1` —— `FID=25`(編輯分區)對 `RID=4`(廠商負責人)與 `RID=5`(廠商組長)。語意自洽:廠商只能編輯自己被指派的分區。

**`A_Right` 無 `RID=1`**(2026-08-31 工程端確認):母帳號不進矩陣,為全開。撰寫 AC 時,Role 1 不套用功能授權層,僅受資料範圍層(其為全視域)約束。

**規模**:`A_Function` 48 列(4 個模組 → 頁面 → 按鈕);`A_Right` 159 列(`RID` 2~7 × `FID`)。皆為已填實的真實資料,非空表。

**下游影響**:
- 撰寫權限相關 AC 時須**分兩句寫**:一句功能可用性(查矩陣),一句資料範圍(Role×GID);`Relation=1` 者在資料範圍句上再加「限帳號關聯資料」。
- `02_Scope.md` 的 `[!TBD-ROLE-02]` **不解除**,改寫為窄追問(見下)。
- L2「`Role<4` 可看所有資料;`Role>3` 依 GID」仍成立,屬資料範圍層,與本決策不衝突。

**保留追問(不阻塞規格撰寫)**:執行時程式是否**真的查詢** `A_Right`,或該矩陣目前為備而未用?
E5 回填答「目前只判定 `OriginUser` 的 Role 來決定」,但該句在上下文中係回答「用哪個 Role 欄位」,未直接回答「是否查矩陣」。矩陣有 159 列自洽實資料,推定為使用中;確認前依本決策撰寫 AC 皆成立,故不列為阻塞。
🔒 解鎖條件:E5 第二輪追問。

- 裁示人:SA｜日期:2026-08-31｜來源:R1 矛盾浮現 C02 / 工程前置決策點 E5
- 依據:E5 回填(Rossi,2026-08-31),含 `A_Function` / `A_Right` 實際資料列,見 `00_START_SA/EngRegister.md`
